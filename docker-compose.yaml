version: "3.8"
services:
  server:
    build: ./server
    ports:
      - "3001:3001"
    restart: on-failure

  client:
    build: ./client
    ports:
      - "3000:3000"
    depends_on:
      - server
    restart: on-failure
